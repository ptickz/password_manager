<h1 align="center">Password manager CLI</h1>

## Usage

Run the following command at the root of project to build a binary executable:

```sh
 go build -o bin/password_manager cmd/password_manager/main.go
```
After that you can start application by following command:

```sh
 bin/password_manager
```